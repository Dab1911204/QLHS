import Modal from "../../common/Model";

const ModelAddEmployee = (onClose,isOpen) => {
    return (
        <Modal title="Thêm nhân viên mới" onClose={onClose} isOpen={isOpen}>

        </Modal>
    )
    
}

export default ModelAddEmployee;